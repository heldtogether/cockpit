# Cockpit

Cockpit is my tmux window manager with notes open on the left (80 chars wide) and a separate tmux session on the right. The outer session is navigated with C-a and then the inner session is navigated with C-b (as usual).

## Installation

1. Clone this repo
2. Add the location of this repo to your $PATH variable
3. `cockpit`